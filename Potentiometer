*
L0--->10 PIN IN digital;
gnd------> gnd;
3v3------>5v;
AIN----->a0;
*/


#define LED_PIN 10
#define POT_PIN A0
void setup() {
Serial.begin(9600);
pinMode( LED_PIN, OUTPUT);
}
void loop() {
// reads the input on analog pin A0(value between 0 and 1023)
int In_POT_Value=analogRead(POT_PIN);
//scales it to brightness(value between 0 and 255)
int Brightness=map(In_POT_Value, 0, 1023, 0, 255);
//sets the brightness LED that connects to pin 3
analogWrite(LED_PIN,Brightness);
//print out the value
if((In_POT_Value>=0 && In_POT_Value<=1023)||(Brightness>=0 && Brightness<=255))
{
Serial.print("POT Value:");
Serial.print(In_POT_Value);
Serial.print(",Brightness value:");
Serial.print(Brightness);
}
else
{
Serial.println("Undefined value");
}
delay(500);
Serial.println("******************************************");
}

Connections:

LED GND pin (shorter leg) connected to GND via a resistor (220Ω or 330Ω).

Potentiometer: One side to 5V, other side to GND, middle pin to A0.



---

Explanation (Practical Working in Simple Words):

1. Potentiometer controls brightness:

As you rotate the knob, voltage on A0 changes (0 to 5V).

Arduino reads this using analogRead(A0) which gives values from 0 to 1023.



2. LED Brightness Control:

Arduino maps this value to 0–255 using map() function.

This value is sent to pin 10 using analogWrite(), which controls the LED brightness using PWM (Pulse Width Modulation).



3. Serial Monitor Output:

Every 0.5 seconds, it prints:

POT value (0–1023)

Brightness (0–255)






---

Would you like a simple diagram to understand the wiring?

